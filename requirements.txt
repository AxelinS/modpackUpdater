# Core runtime deps
customtkinter>=5.2.0
httpx>=0.27.0
orjson>=3.10.0     # optional but recommended; falls back to stdlib json
nuitka>=2.3.0
# Build tools (not needed at runtime)
# pyinstaller>=6.6.0
# 
